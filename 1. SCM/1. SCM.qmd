---
title: 'SCM'
formt: html
---

> Here I want to show the code to run a regression:

```{r}
library(wooldridge)

data("wage1")

model <- lm(wage ~ educ + exper + tenure, data = wage1)

summary(model)
```

Interpretation:
As you can see, there are different coefficients.
Also
